/**
 * Legger sammen to tall og returnerer summen
 * @param a Det første tallet
 * @param b Det andre tallet
 * @returns Summen av de to tallene
 */
export function addNumbers(a: number, b: number): number {
  return a + b;
}

// Eksempel på bruk:
// const sum = addNumbers(5, 3);  // Returnerer 8